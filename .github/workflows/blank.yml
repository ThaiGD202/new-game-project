name: Build iOS

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Download Godot
      run: |
        wget https://github.com/godotengine/godot/releases/download/3.5.3-stable/Godot_v3.5.3-stable_osx.universal.zip
        unzip Godot_v3.5.3-stable_osx.universal.zip -d godot
        sudo mv godot/Godot.app /Applications/Godot.app
        sudo ln -s /Applications/Godot.app/Contents/MacOS/Godot /usr/local/bin/godot

    - name: Download Godot Export Templates
      run: |
        mkdir -p ~/Library/Application\ Support/Godot/templates/3.5.3.stable
        wget https://github.com/godotengine/godot/releases/download/3.5.3-stable/Godot_v3.5.3-stable_export_templates.tpz
        unzip Godot_v3.5.3-stable_export_templates.tpz -d temp_templates
        mv temp_templates/templates/* ~/Library/Application\ Support/Godot/templates/3.5.3.stable/
        rm -rf temp_templates Godot_v3.5.3-stable_export_templates.tpz

    - name: Create build directory
      run: mkdir -p build

    - name: Create export_presets.cfg automatically
      run: |
        cat <<EOF > export_presets.cfg
        [preset.0]
        name="iOS"
        platform="iOS"
        runnable=true
        custom_features=""
        export_filter="all_resources"
        include_filter=""
        exclude_filter=""
        export_path="build/your_project_name.xcodeproj"

        [preset.0.options]
        custom_template/debug=""
        custom_template/release=""
        application/target_id_sdk="iphoneos"
        application/bundle_id="com.mycompany.mygame"
        application/signature=""
        application/short_version_string="1.0"
        application/version_code="1"
        application/name_en=""
        architectures/arm64=true
        application/provisioning_profile_uuid=""
        application/app_store_team_id="12345ABCDE"
        privacy/camera_usage_description=""
        privacy/microphone_usage_description=""
        privacy/photolibrary_usage_description=""
        EOF



    - name: Export iOS project
      run: godot --export "iOS" build/your_project_name.xcodeproj
      env:
        DISPLAY: :99.0
        
    - name: Set iOS Deployment Target
      run: sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = 10.0;/IPHONEOS_DEPLOYMENT_TARGET = 12.0;/' build/your_project_name.xcodeproj/project.pbxproj

    - name: Build iOS project
      run: xcodebuild -project build/your_project_name.xcodeproj -scheme your_project_name -configuration Development -archivePath build/your_project_name.xcarchive archive CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO

    - name: Export IPA
      run: | 
        cd build/your_project_name.xcarchive/Products/Applications
        mkdir Payload
        mv your_project_name.app Payload/
        zip -r ../../../App.ipa Payload/

    - name: Upload IPA to GitHub Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: Ipa
        path: build/App.ipa
