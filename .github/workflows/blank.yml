name: Build iOS

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Godot 4 via Homebrew
      run: |
        # Cài đặt Godot Engine trực tiếp qua hệ thống Homebrew của macOS
        brew install --cask godot
        # Tạo liên kết dòng lệnh để hệ thống gọi được lệnh godot công khai
        sudo ln -s /Applications/Godot.app/Contents/MacOS/Godot /usr/local/bin/godot

    - name: Download Godot 4.3 Export Templates Official
      run: |
        mkdir -p ~/Library/Application\ Support/Godot/export_templates/4.3.stable
        # Sử dụng cờ giả lập trình duyệt để tránh bị GitHub block lượt tải
        curl -A "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)" -L -o templates.tpz https://github.com
        unzip templates.tpz -d temp_templates
        mv temp_templates/templates/* ~/Library/Application\ Support/Godot/export_templates/4.3.stable/
        rm -rf temp_templates templates.tpz


    - name: Create build directory
      run: mkdir -p build

    - name: Create export_presets.cfg automatically (Godot 4)
      run: |
        cat <<EOF > export_presets.cfg
        [preset.0]
        name="iOS"
        platform="iOS"
        runnable=true
        dedicated_server=false
        custom_features=""
        export_filter="all_resources"
        include_filter=""
        exclude_filter=""
        export_path="build/your_project_name.xcodeproj"

        [preset.0.options]
        architectures/arm64=true
        application/app_store_team_id="12345ABCDE"
        application/provisioning_profile_uuid_debug=""
        application/provisioning_profile_uuid_release=""
        application/bundle_identifier="com.mycompany.mygame"
        application/signature=""
        application/short_version_string="1.0"
        application/version_code="1"
        application/name_en=""
        user_agent=""
        privacy/camera_usage_description="Camera access"
        privacy/microphone_usage_description="Microphone access"
        privacy/photolibrary_usage_description="Photo library access"
        EOF

    - name: Export iOS project
      run: godot --headless --export-debug "iOS" build/your_project_name.xcodeproj
        
    - name: Set iOS Deployment Target
      run: sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = 11.0;/IPHONEOS_DEPLOYMENT_TARGET = 13.0;/' build/your_project_name.xcodeproj/project.pbxproj

    - name: Build iOS project
      run: xcodebuild -project build/your_project_name.xcodeproj -scheme your_project_name -configuration Development -archivePath build/your_project_name.xcarchive archive CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO

    - name: Export IPA
      run: | 
        cd build/your_project_name.xcarchive/Products/Applications
        mkdir Payload
        mv your_project_name.app Payload/
        zip -r ../../../App.ipa Payload/

    - name: Upload IPA to GitHub Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: Ipa
        path: build/App.ipa
